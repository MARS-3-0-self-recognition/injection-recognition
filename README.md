1. Install package using `pip install -e .`
2. Install requirements using `pip install -r requirements.txt`
3. To run the control and treatment experiments, run scripts/control_and_treatment_experiments.ipynb 

